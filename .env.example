# TAK Manager — Environment Configuration
# Copy to .env and edit before deploying: cp .env.example .env

# ── Domain (single-host routing) ────────────────────────────────────────────

# Public domain for UI and API (API is path-routed at /api)
DOMAIN=data.opengeo.space

# ── Backend ────────────────────────────────────────────────────────────────

# Log verbosity: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# CORS allowed origins — include the public app origin.
CORS_ORIGINS=["https://data.opengeo.space"]

# ── Auth ───────────────────────────────────────────────────────────────────

# When true (default), backend trusts Authentik forward-auth headers
# (x-authentik-username / x-authentik-groups). Requests missing them → 401.
# Set to false for private LAN deploys without Authentik: backend skips header
# checks and treats every caller as admin. ONLY safe behind a firewall/VPN.
AUTH_ENABLED=true
